displayName: Block Path
summary: Blocks HTTP requests with a path matching the configured regex.
type: middleware
import: github.com/samson4649/plugin-blockpath
compatibility: TODO
testData:
  elements:
    - regex: ^/path
      code: 403 